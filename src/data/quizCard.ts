export type QuizMeta = {
  id: string;
  title: string;
  duration: number; // dalam detik
};

export const quizMeta: QuizMeta = {
  id: "skd-paket-1",
  title: "SKD CPNS PAKET 1",
  duration: 100 * 60, // 100 menit
};
